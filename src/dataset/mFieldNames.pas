unit mFieldNames;

{$IFDEF FPC}
  {$MODE DELPHI}
{$ENDIF}

interface

const
  SEPARATOR_FIELDS_FROM_JOIN = '$';
  SEPARATOR_FIELDS_FROM_INTERNAL_REFERENCE = '#';
  PREFIX_SYSTEM_FIELDS = '_';

implementation

end.
